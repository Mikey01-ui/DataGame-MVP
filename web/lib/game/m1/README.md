# Mission 1 — React game engine

Port mechanics from `reference/legacy-missions/Mission 1.html` into this folder:

- `types.ts` — GS state, lead nodes, detection
- `engine.ts` — scoring, verification, lead progression
- `data.ts` or load from `content/missions/m1/game-data.json`

UI shells live in `components/missions/m1/` (add when desktop gameplay is ready).
